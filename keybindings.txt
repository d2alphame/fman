Some keybindings ideas

Mod + m = Global prefix
fman is modal. Meaning it has various modes.

Available keyboard shortcuts depend on the current mode. However
some shortcuts are always available irrespective of the mode

In main mode:
tab to switch panes

